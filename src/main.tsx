import React,{useEffect,useState} from 'react';
import{createRoot}from'react-dom/client';
import{createClient,Session}from'@supabase/supabase-js';
import'./style.css';
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_ANON_KEY);
type Opp={id:string;title:string;stage:string;opportunity_score:number|null;confidence_score:number|null;data_quality_score:number|null;company_id:string;contact_id?:string|null};
type GuardState={decision:string;reason:string;at:string};type Action={id:string;opportunity_id:string|null;status:string;action_type:string;execution_provider?:string|null;execution_mode?:string|null;execution_result?:any};
function App(){const[session,setSession]=useState<Session|null>(null),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[tenant,setTenant]=useState<any>(null),[opps,setOpps]=useState<Opp[]>([]),[busy,setBusy]=useState(false),[guards,setGuards]=useState<Record<string,GuardState>>({}),[actions,setActions]=useState<Action[]>([]);
useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>subscription.unsubscribe()},[]);useEffect(()=>{if(session)load()},[session]);
async function login(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);setBusy(false)}
async function load(){setError('');const{data:m,error:me}=await supabase.from('tenant_members').select('tenant_id,role,tenants(id,name,slug)').limit(1).single();if(me){setError(me.message);return}setTenant(m);const[{data:o,error:oe},{data:a,error:ae}]=await Promise.all([supabase.from('opportunities').select('id,title,stage,opportunity_score,confidence_score,data_quality_score,company_id,contact_id').order('created_at',{ascending:false}),supabase.from('actions').select('id,opportunity_id,status,action_type,execution_provider,execution_mode,execution_result').order('created_at',{ascending:false})]);if(oe)setError(oe.message);else setOpps(o||[]);if(ae)setError(ae.message);else setActions(a||[])}
function current(oid:string){return actions.find(a=>a.opportunity_id===oid&&['pending_approval','approved','running','completed'].includes(a.status))}
async function guard(o:Opp){if(!tenant)return;const existing=current(o.id);if(existing){setGuards(g=>({...g,[o.id]:{decision:existing.status==='pending_approval'?'review':'allow',reason:'Action existante détectée.',at:new Date().toISOString()}}));return}if(!o.contact_id){setGuards(g=>({...g,[o.id]:{decision:'block',reason:'Contact manquant.',at:new Date().toISOString()}}));return}const{data,error}=await supabase.rpc('guard_outreach',{p_tenant:tenant.tenant_id,p_contact:o.contact_id,p_agent:'sales',p_action:'external.send'});if(error){setError(error.message);return}const d=String(data).toLowerCase();setGuards(g=>({...g,[o.id]:{decision:d,reason:d==='review'?'Validation humaine requise avant toute action externe.':'Décision Guard : '+d,at:new Date().toISOString()}}));if(d==='review'){await supabase.from('actions').insert({tenant_id:tenant.tenant_id,opportunity_id:o.id,contact_id:o.contact_id,agent_key:'sales',action_type:'external.send',status:'pending_approval',payload:{source:'control_center'}});await load()}}
async function decide(id:string,d:'approve'|'reject'){const{error}=await supabase.rpc('decide_action',{p_action:id,p_decision:d,p_note:d==='approve'?'Validé depuis Orixeo Control Center':'Refusé depuis Orixeo Control Center'});if(error)setError(error.message);else load()}
async function simulate(a:Action,provider:'brevo'|'hostinger_mail'){setError('');const{error:p}=await supabase.rpc('prepare_action_execution',{p_action:a.id,p_channel:'email',p_provider:provider,p_mode:'simulation'});if(p){setError(p.message);return}const{error:e}=await supabase.rpc('simulate_action_execution',{p_action:a.id});if(e)setError(e.message);else load()}
if(!session)return <main className="login"><section><div className="brand">ORIXEO <b>LAB</b></div><h1>Control Center</h1><p>Votre équipe IA opérationnelle.</p><form onSubmit={login}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Mot de passe" value={password} onChange={e=>setPassword(e.target.value)} required/><button disabled={busy}>{busy?'Connexion…':'Se connecter'}</button></form>{error&&<div className="err">{error}</div>}</section></main>;
const pending=actions.filter(a=>a.status==='pending_approval'),approved=actions.filter(a=>a.status==='approved');return <div><header><div className="brand">ORIXEO <b>LAB</b></div><div>{tenant?.tenants?.name} · {tenant?.role}<button className="link" onClick={()=>supabase.auth.signOut()}>Déconnexion</button></div></header><main className="wrap"><div className="hero"><div><span className="tag">CONTROL CENTER · V0.4 · SPRINT 1.5</span><h1>Execution Engine</h1><p>Brevo + Hostinger Mail, d'abord en simulation contrôlée.</p></div><button onClick={load}>Actualiser</button></div>{error&&<div className="err">{error}</div>}<div className="cards"><article><strong>{opps.length}</strong><span>Opportunités</span></article><article><strong>{pending.length}</strong><span>À valider</span></article><article><strong>{approved.length}</strong><span>Prêtes à exécuter</span></article></div>{pending.length>0&&<section className="panel approvalPanel"><h2>À valider</h2>{pending.map(a=><div className="approval" key={a.id}><strong>{opps.find(o=>o.id===a.opportunity_id)?.title}</strong><div><button onClick={()=>decide(a.id,'approve')}>Approuver</button><button className="reject" onClick={()=>decide(a.id,'reject')}>Refuser</button></div></div>)}</section>}{approved.length>0&&<section className="panel approvalPanel"><h2>Execution Engine — simulation</h2><p className="muted">Aucun email réel ne sera envoyé pendant ce test.</p>{approved.map(a=><div className="approval" key={a.id}><div><strong>{opps.find(o=>o.id===a.opportunity_id)?.title}</strong><span>Choisir le futur canal d'exécution</span></div><div><button onClick={()=>simulate(a,'brevo')}>Simuler Brevo</button><button onClick={()=>simulate(a,'hostinger_mail')}>Simuler Hostinger Mail</button></div></div>)}</section>}<section className="panel"><h2>Opportunity Engine</h2><div className="table"><div className="row head"><span>Opportunité</span><span>Étape</span><span>Score</span><span>Confiance</span><span>Qualité</span><span>État</span></div>{opps.map(o=>{const a=current(o.id),g=guards[o.id],state=a?.status||g?.decision;return <div className="row" key={o.id}><span>{o.title}</span><span className="pill">{o.stage}</span><span>{o.opportunity_score??'—'}</span><span>{o.confidence_score??'—'}</span><span>{o.data_quality_score??'—'}</span><span>{state?<span className={'guard '+(state==='approved'||state==='completed'?'allow':state)}>{state.toUpperCase()}</span>:<button className="small" onClick={()=>guard(o)}>Tester</button>}</span></div>})}</div></section></main></div>}
createRoot(document.getElementById('root')!).render(<App/>);