import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient,Session}from'@supabase/supabase-js';import'./style.css';
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_ANON_KEY);
type Opp={id:string;title:string;stage:string;opportunity_score:number|null;confidence_score:number|null;data_quality_score:number|null;offer_id?:string|null;ai_analysis?:any};
type Job={id:string;opportunity_id:string;status:string;progress:number;engine_version?:string;result?:any;error?:string;cost_usd?:number;cache_hit?:boolean;facts_reused?:number};
type Fact={id:string;opportunity_id:string;field_key:string;value:any;source_uri?:string;confidence:number;relevance_score?:number;relevance_status?:string;status:string;observed_at?:string};
function App(){const[session,setSession]=useState<Session|null>(null),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[notice,setNotice]=useState(''),[opps,setOpps]=useState<Opp[]>([]),[jobs,setJobs]=useState<Job[]>([]),[facts,setFacts]=useState<Fact[]>([]),[offers,setOffers]=useState<any[]>([]),[metrics,setMetrics]=useState<any>(null),[busy,setBusy]=useState<string|null>(null);
useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>subscription.unsubscribe()},[]);useEffect(()=>{if(session)load()},[session]);useEffect(()=>{if(!session||!jobs.some(j=>['queued','running','verifying'].includes(j.status)))return;const t=setInterval(loadJobs,2500);return()=>clearInterval(t)},[session,jobs]);
async function login(e:React.FormEvent){e.preventDefault();const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message)}
async function loadJobs(){const{data}=await supabase.from('enrichment_jobs').select('id,opportunity_id,status,progress,engine_version,result,error,cost_usd,cache_hit,facts_reused').order('created_at',{ascending:false}).limit(100);const j=(data||[])as Job[];setJobs(j);if(busy){const x=j.find(v=>v.opportunity_id===busy);if(x?.status==='completed'){setBusy(null);setNotice(`Enrichissement terminé · score serveur ${x.result?.scoring?.score??'—'}/100 · ${x.cache_hit?'cache utilisé':'recherche effectuée'}.`);await load()}else if(x?.status==='failed'){setBusy(null);setError(`Enrichissement échoué : ${x.error||'erreur inconnue'}`)}}}
async function load(){const[{data:o},{data:j},{data:f},{data:of},{data:m}]=await Promise.all([supabase.from('opportunities').select('id,title,stage,opportunity_score,confidence_score,data_quality_score,offer_id,ai_analysis').order('created_at',{ascending:false}),supabase.from('enrichment_jobs').select('id,opportunity_id,status,progress,engine_version,result,error,cost_usd,cache_hit,facts_reused').order('created_at',{ascending:false}).limit(100),supabase.from('enrichment_facts').select('id,opportunity_id,field_key,value,source_uri,confidence,relevance_score,relevance_status,status,observed_at').in('status',['applied','observed']).order('observed_at',{ascending:false}).limit(300),supabase.from('offers').select('id,name').eq('active',true),supabase.from('enrichment_cost_summary').select('*').maybeSingle()]);setOpps(o||[]);setJobs((j||[])as Job[]);setFacts((f||[])as Fact[]);setOffers(of||[]);setMetrics(m||null)}
function job(id:string){return jobs.find(j=>j.opportunity_id===id)}function offer(id?:string|null){return offers.find(x=>x.id===id)?.name||'—'}function selectedFacts(id:string){return facts.filter(f=>f.opportunity_id===id&&['buying_signal','timing'].includes(f.field_key)&&['selected','candidate'].includes(f.relevance_status||'')).sort((a,b)=>(b.relevance_score||0)-(a.relevance_score||0)).slice(0,3)}
async function analyze(o:Opp){setBusy(o.id);setError('');const{data,error}=await supabase.functions.invoke('analyze-opportunity',{body:{opportunity_id:o.id}});setBusy(null);if(error||!data?.ok)return setError(data?.error||error?.message||'Analyse impossible');setNotice(`Scoring serveur recalculé : ${data.score}/100 · confiance ${data.confidence}/100.`);await load()}
async function enrich(o:Opp){setBusy(o.id);setError('');const{data,error}=await supabase.functions.invoke('enqueue-enrichment',{body:{opportunity_id:o.id}});if(error||!data?.ok){setBusy(null);return setError(data?.error||error?.message||'Enrichissement impossible')}await loadJobs();setNotice(data.deduplicated?'Un job identique est déjà actif.':'Enrichissement durable lancé · suivi automatique actif.')}
if(!session)return <main className="login"><section><img className="loginLogo" src="/orixeo-logo.svg"/><h1>Control Center</h1><form onSubmit={login}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mot de passe"/><button>Se connecter</button></form>{error&&<div className="err">{error}</div>}</section></main>;
const ready=opps.filter(o=>o.ai_analysis?.commercial_status==='ready_for_review'),needs=opps.filter(o=>['needs_enrichment','needs_qualification'].includes(o.ai_analysis?.commercial_status)),blocked=opps.filter(o=>o.ai_analysis?.commercial_status==='do_not_contact');return <div className="appShell"><aside className="sidebar"><img className="logo" src="/orixeo-logo.svg"/><nav><a className="active">⌂ <span>Control Center</span></a><a>◎ <span>Opportunités</span></a><a>⌕ <span>Enrichissement</span></a><a>▥ <span>Analytics</span></a><a>◷ <span>Journal d’activité</span></a></nav><button className="logout" onClick={()=>supabase.auth.signOut()}>Déconnexion</button></aside><main className="dashboard"><div className="topbar"><div><h1>CONTROL CENTER</h1><p>V0.9.0 · ORIXEO QUALITY & COST 1.9.2</p></div><button className="secondary" onClick={load}>↻ Actualiser</button></div>{error&&<div className="err">{error}</div>}{notice&&<div className="notice">{notice}</div>}<div className="kpis"><article><i>◎</i><strong>{opps.length}</strong><span>Opportunités<br/>total</span></article><article><i>↗</i><strong>{ready.length}</strong><span>Prêtes à contacter</span></article><article className="warn"><i>♙</i><strong>{needs.length}</strong><span>À enrichir</span></article><article><i>$</i><strong>{Number(metrics?.total_cost_usd||0).toFixed(3)}</strong><span>Coût LLM cumulé<br/>USD</span></article><article><i>↻</i><strong>{metrics?.cache_hit_rate??0}%</strong><span>Taux de cache<br/>{metrics?.facts_reused??0} preuves réutilisées</span></article></div><section className="panel engine"><div className="sectionTitle"><h2>OPPORTUNITY ENGINE</h2><small>Proof Engine · Quality & Cost</small></div>{opps.map(o=>{const a=o.ai_analysis||{},j=job(o.id),working=j&&['queued','running','verifying'].includes(j.status),sf=selectedFacts(o.id),status=a.commercial_status==='ready_for_review'?'ready':['needs_enrichment','needs_qualification'].includes(a.commercial_status)?'enrich':a.commercial_status==='do_not_contact'?'blocked':'unscored';return <div className="oppWrap" key={o.id}><div className="oppRow"><div><strong>{o.title}</strong><small>{offer(o.offer_id)}</small></div><div><span className={'status '+status}>{status==='ready'?'PRÊTE À CONTACTER':status==='enrich'?'À ENRICHIR':status==='blocked'?'CONTACT INTERDIT':'À RECALCULER'}</span><small>{working?`${j?.status.toUpperCase()} ${j?.progress}%`:j?.status==='completed'?'TERMINÉ 100%':'—'}</small></div><div className={'score '+status}><b>{o.opportunity_score??'—'}</b><small>/100</small></div><div className="metric"><b>{o.confidence_score??'—'}</b><small>/100 confiance</small></div><div className="metric"><b>{a.data_completeness??'—'}</b><small>/100 complétude</small></div><div className="metric"><b>{j?.cache_hit?'OUI':'NON'}</b><small>cache · {j?.facts_reused||0} réutilisée(s)</small></div><div><button disabled={!!working||status==='blocked'} className="enrichBtn" onClick={()=>enrich(o)}>{working?`⌛ ${j?.progress}%`:'↻ Ré-enrichir'}</button><button className="detail" disabled={busy===o.id} onClick={()=>analyze(o)}>Recalculer</button></div></div>{j?.status==='completed'&&<div className="qualityStrip"><span><b>Coût</b> ${Number(j.cost_usd||j.result?.usage?.cost||0).toFixed(4)}</span><span><b>Cache</b> {j.cache_hit?'✓ utilisé':'—'}</span><span><b>Preuves réutilisées</b> {j.facts_reused??j.result?.reused?.length??0}</span><span><b>Moteur</b> {j.result?.engine_version||j.engine_version||'—'}</span></div>}{sf.length>0&&<details className="evidenceDetails"><summary>Preuves commerciales retenues · {sf.length}</summary>{sf.map(f=><div className="evidence" key={f.id}><div><b>{f.field_key==='buying_signal'?'Signal d’achat':'Timing'}</b><p>{String(f.value)}</p></div><div className="evidenceScores"><span>Pertinence <b>{f.relevance_score??'—'}/100</b></span><span>Confiance <b>{Math.round(Number(f.confidence)*100)}/100</b></span><span className="proof">{f.relevance_status==='selected'?'✓ SÉLECTIONNÉ':'CANDIDAT'}</span></div>{f.source_uri&&<a href={f.source_uri} target="_blank" rel="noreferrer">Voir la source ↗</a>}</div>)}</details>}</div>})}</section><div className="legend"><span><i className="green"></i> Prête à contacter</span><span><i className="orange"></i> À enrichir</span><span><i className="red"></i> Contact interdit</span><aside><b>ⓘ Orixeo 1.9.2 Quality & Cost</b><br/>Preuves vérifiées · cache intelligent · coût mesuré · scoring serveur</aside></div></main></div>}
createRoot(document.getElementById('root')!).render(<App/>);